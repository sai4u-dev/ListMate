import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
module.exports = {
  images: {
    domains: ['www.istockphoto.com', 'media.istockphoto.com', 'via.placeholder.com'], // Add your domains here
  },
}